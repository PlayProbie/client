/**
 * lib directory index
 */
export { cleanupMockStream,createMockMediaStream } from './mock-stream';
export type {
  StreamClient,
  StreamClientConfig,
  StreamInputEvent,
} from './stream-client';
export { createStreamClient } from './stream-client';
